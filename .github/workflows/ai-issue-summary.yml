# =============================================================================
# AI Issue Summary
# =============================================================================
# Generiert KI-gestützte Zusammenfassungen für neue Issues und Pull Requests.
# Nutzt die Reusable-Workflows aus bauer-group/automation-templates.
# =============================================================================

name: AI Issue Summary

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

permissions:
  issues: write
  pull-requests: write
  contents: read
  models: read

jobs:
  summarize:
    name: Generate AI Summary
    uses: bauer-group/automation-templates/.github/workflows/modules-ai-issue-summary.yml@main
    with:
      summary-type: "technical"
      add-labels: true
      add-priority: true
      translate: ""
      comment-template: |
        ## AI Analysis

        {{ summary }}

        ---
        <sub>Generated by AI Issue Summary</sub>
    secrets: inherit
